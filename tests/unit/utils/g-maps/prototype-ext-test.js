import gMapsPrototypeExt from '../../../utils/g-maps/prototype-ext';
import { module, test } from 'qunit';

module('Unit | Utility | g maps/prototype ext');

// Replace this with your real tests.
test('it works', function(assert) {
  var result = gMapsPrototypeExt();
  assert.ok(result);
});
