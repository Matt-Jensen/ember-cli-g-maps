import gMapsConfigurator from '../../../utils/g-maps/configurator';
import { module, test } from 'qunit';

module('Unit | Utility | g maps/configurator');

// Replace this with your real tests.
test('it works', function(assert) {
  var result = gMapsConfigurator();
  assert.ok(result);
});
